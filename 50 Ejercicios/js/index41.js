// 41. Vocal
letra = prompt("Ingrese una letra:")

if (letra === "a" || letra === "e" || letra === "i" || letra === "o" || letra === "u") {
    alert("Es vocal")
} else {
    alert("No es vocal")
}
